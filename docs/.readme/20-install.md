## Install

```bash
npm install {{NAME}}
# or
pnpm add {{NAME}}
# or
yarn add {{NAME}}
```

CLI (via npx or global install):

```bash
npx {{NAME}}          # run once without installing
# or
npm install -g {{NAME}}
{{NAME}}              # launch the interactive CLI
```
